# 示例集团 员工职业性格测评系统 —— AI 运维交接包

本压缩包是用于把项目**全权交接给未来运维 AI** 的自包含资料。包含：

1. `AI运维总览手册.md` —— 核心。项目全过程、全部文件职责、本地/云端/远端三重环境、
   部署机制（链接永不变铁律）、Bug 修复史、数据恢复复盘、操作 Runbook、遗留任务、
   给 AI 的授权红线与标准交接 Prompt。AI 必须先通读本文件。
2. `src/` —— 完整前端源码（22 个文件，已含全部修复）。
3. 根目录入口 HTML + 关键配置（package.json / vite.config.js / 8 个构建入口页）。
4. 参考文档：`使用说明操作手册.md`、`数据恢复指引.html`、`职业岗位画像参考手册.html`。

## 使用方法（给未来 AI）
- 解压本包，先读 `AI运维总览手册.md`。
- 源码已在 `src/`，可直接阅读修改；如需与远端同步，仓库为
  `git@github.com:intp41455/career-personality-assessment.git`（私有，分支 main）。
- 严格遵循手册第 13 节红线：**更新部署只同目录重部署，绝不下架链接**。

## 已主动排除（不纳入本包，避免泄露/冗余）
- `node_modules/`、`.deploy-build-*/`、`dist/`、`.git/`
- `new-session-*.json`（含历史会话存档，有泄露风险）
- `test_dims.mjs` / `gen_job_profiles.mjs` / `FULL_SOURCE_CODE.md` / `格式校验报告_*` 等临时产物
- 孤立模块 `src/modules/finance/bank_payment_gateway.js`（全仓库无引用，待核查删除）

生成时间：2026-08-25 16:04
